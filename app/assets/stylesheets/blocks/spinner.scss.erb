.spinner-wrapper {
  display: none;
  &.active { display: block; }
}
.page-cover {
  position: fixed; left: 0; top: 0; height: 100%; width: 100%; background: #fff; opacity: .5; display: none; z-index: 1000;
  &.active { display: block }
}
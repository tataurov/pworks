app.modules.root = (function(self) {

  // Инициализация событий
  function _addEvents(){

  }

  // Инициализация модуля
  function _init(){

  }

  self.load = function() {
    _init();
    _addEvents();
  };

  return self;

}(app.modules.root || {}));